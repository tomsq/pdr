# pdr

**Plan, Delegate, Review.** A [Claude Code](https://claude.com/claude-code) workflow: the main session plans and reviews, Sonnet `implementer` subagents write the code.

## Install

```sh
claude plugin marketplace add tomsq/pdr
claude plugin install pdr@tomsq
```

Use `/pdr` in any session.
