{
  "name": "pdr",
  "version": "0.1.0",
  "description": "Plan, Delegate, Review: an orchestrator plans, specs, and reviews while Sonnet implementer subagents write the code. Built on the Claude Agent SDK.",
  "type": "module",
  "bin": {
    "pdr": "./bin/pdr.js"
  },
  "files": [
    "bin",
    "skills",
    "agents",
    "README.md"
  ],
  "scripts": {
    "sync-plugin-version": "node scripts/check-plugin-version.mjs",
    "check-plugin-version": "node scripts/check-plugin-version.mjs --check"
  },
  "engines": {
    "node": ">=18"
  },
  "repository": "github:tomsq/pdr",
  "keywords": [
    "claude",
    "agent",
    "claude-code",
    "orchestrator",
    "delegation"
  ],
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.247"
  }
}
